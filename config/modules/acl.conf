# ACL Counter 模块配置

# 是否启用此模块
enabled=true

# SONiC中的NAME_MAP路径（固定）
name_map_path="ACL_COUNTER_RULE_MAP"

# Prometheus指标前缀
prometheus_prefix="sonic_acl"

# 生成的标签名
label_name="acl_rule"

# 名称过滤（正则表达式，留空表示不过滤）
name_filter=""